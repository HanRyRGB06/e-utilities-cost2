name: CD Pipeline

on:
  push:
    branches: [ "main" ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout Code
      uses: actions/checkout@v3

    - name: Mock Deploy to Server
      run: |
        echo "This is a mock deployment."
        echo "To deploy to a real server, add the SSH action back and configure SERVER_HOST, SERVER_USER, and SERVER_SSH_KEY in GitHub Secrets."
        echo "Deployment successful!"
